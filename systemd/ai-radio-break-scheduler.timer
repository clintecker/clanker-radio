[Unit]
Description=AI Radio Station - Break Scheduler Timer
Requires=ai-radio-break-scheduler.service

[Timer]
# Fire during minute :59 exactly; schedule_break.py only acts then (a */5 timer never hits :59).
OnCalendar=*:59:00
AccuracySec=1s

Persistent=true

[Install]
WantedBy=timers.target
